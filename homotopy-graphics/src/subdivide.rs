use homotopy_core::mesh::{Mesh};

/// Subdivide square mesh one time
pub fn subdivide3(control_mesh: Mesh) -> Mesh {
    return control_mesh;
}

/// Subdivide cubical mesh one time
pub fn subdivide4(control_mesh: Mesh) -> Mesh {
    return control_mesh;
}